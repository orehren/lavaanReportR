% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_estimates_data.R
\name{create_model_estimates_table}
\alias{create_model_estimates_table}
\title{Create a Formatted Table of Parameter Estimates}
\usage{
create_model_estimates_table(data_object, ...)
}
\arguments{
\item{data_object}{A `data.table` as produced by `extract_model_estimates_data()`.}

\item{table_format}{A character string, either `"long"` (default) or `"wide"`.
`"long"` format uses row groups for sections, while `"wide"` format
reshapes the data to use column spanners.}

\item{groupname_col}{Tidyselect expression for the column to be used for `gt`'s
row groups. Defaults to `Section`. Only used if `table_format` is `"long"`.}

\item{rowname_col}{Tidyselect expression for the column to be used for `gt`'s
row stubs. Defaults to `Measure` in long format and `Type` in wide format.}

\item{col_names}{An optional named list or vector for relabeling columns,
passed to `gt::cols_label()`. Example: `list(default = "Value")`.}

\item{hide_cols}{Optional. A character vector or tidyselect expression of
columns to hide from the final table.}

\item{align_decimal}{Logical. If `TRUE`, numeric columns will be aligned at
the decimal point. Defaults to `FALSE`.}

\item{decimals}{The number of decimal places to display for numeric values.}

\item{substitute_pvalues}{Logical. If `TRUE`, numeric p-values are formatted
into character strings (e.g., "< .001"). Defaults to `TRUE`.}

\item{table_caption}{An optional character string for the table's main title.}

\item{source_note}{An optional character string for the table's source note.}

\item{footnote_specs}{An optional list of footnote specifications. Use `"default"`
to apply a set of predefined, context-aware footnotes. To add custom
footnotes, provide a list of specifications created with
`gt_create_footnote_spec()`. Set to `NULL` to disable all footnotes.}

\item{save_output_to_file}{Logical. If `TRUE`, the `gt` table will be saved.}

\item{output_file_path}{The directory path for saving the table.}

\item{output_file_name}{The base name for the file to save.}

\item{output_format}{The format for the saved table (e.g., "html", "docx").}
}
\value{
A `gt_tbl` object, ready for printing or saving.
}
\description{
Takes a `data.table` from `extract_model_estimates_data` and creates a
publication-ready `gt` table using a flexible configuration system. This
function acts as a user-friendly wrapper around a generic table-making engine.
}
\examples{
if (requireNamespace("lavaan", quietly = TRUE)) {
  HS.model <- ' visual  =~ x1 + x2 + x3
                textual =~ x4 + x5 + x6
                speed   =~ x7 + x8 + x9 '
  fit <- lavaan::cfa(HS.model, data = lavaan::HolzingerSwineford1939)
  estimates_data <- extract_model_estimates_data(fit)

  # Create a table with custom column names and a caption
  \dontrun{
  estimates_data <- create_model_estimates_table(
    estimates_data,
    table_caption = "CFA Parameter Estimates",
    col_names = list(
      rhs = " ",
      est.unstd = "B",
      se.unstd = "SE",
      pvalue.unstd = "p",
      est.std = "Beta"
    ),
    hide_cols = c(z.unstd, z.std, pvalue.std)
  )
  # print(estimates_data)
  }
}
}
