% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_estimates_data.R
\name{extract_model_estimates_data}
\alias{extract_model_estimates_data}
\title{Extract Parameter Estimates from a `lavaan` Model}
\usage{
extract_model_estimates_data(
  lavaan_object,
  estimates_type = "both",
  ci = TRUE,
  ci_level = 0.95,
  output = "all",
  save_output_to_file = FALSE,
  output_file_path = NULL,
  output_file_name = "model_estimates_data",
  output_format = "rds"
)
}
\arguments{
\item{lavaan_object}{A fitted model object of class `lavaan`.}

\item{estimates_type}{A character string specifying which estimates to return.
One of `"both"` (default), `"unstandardized"`, or `"standardized"`.}

\item{ci}{Logical. If `TRUE` (default), confidence intervals are included.}

\item{ci_level}{A single numeric value between 0 and 1 for the confidence
level of intervals. Defaults to `0.95`.}

\item{output}{A character vector specifying which parameter types to include.
Defaults to `"all"`. Allowed values are: `"Latent Variables"`,
`"Regression Paths"`, `"Variances"`, `"Covariances"`, `"Intercepts"`,
`"Defined Parameters"`.}

\item{save_output_to_file}{Logical. If `TRUE`, the resulting `data.table`
will be saved to a file.}

\item{output_file_path}{The directory path for saving the data.}

\item{output_file_name}{The base name for the output file (without extension).}

\item{output_format}{The format for the saved data, one of "csv", "rds", "xlsx".}
}
\value{
A `data.table` containing the selected parameter estimates. The table
  has attributes `estimates_included` and `ci_included` for use by downstream
  formatting functions.
}
\description{
Extracts a comprehensive set of parameter estimates (unstandardized and/or
standardized), standard errors, p-values, and confidence intervals from a
fitted `lavaan` model object. The function returns a clean `data.table`
ready for formatting.
}
\examples{
if (requireNamespace("lavaan", quietly = TRUE)) {
  HS.model <- ' visual  =~ x1 + x2 + x3
                textual =~ x4 + x5 + x6
                speed   =~ x7 + x8 + x9 '
  fit <- lavaan::cfa(HS.model, data = lavaan::HolzingerSwineford1939)

  # Extract only standardized regression paths without CIs
  estimates <- extract_model_estimates_data(
    fit,
    estimates_type = "standardized",
    ci = FALSE,
    output = "Regression Paths"
  )
  print(estimates)
}
}
